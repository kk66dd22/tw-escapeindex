
## 桌機兩欄滿版驗證（2026-08-19）

1280px 截圖確認目錄容器採寬版配置，卡片區使用兩欄排列；768px 與 375px 維持單欄，Hero、篩選列、搜尋與卡片內容沒有出現整頁水平溢出。程式化 Grid 測試回傳 1280px 為 2 欄、375px 為 1 欄，搜尋／恐怖篩選／恐怖排序／分頁回歸均通過。後續保存版本前仍需完成 todo 標記與最後一次 build／視覺確認。


## 真正桌機滿版修正驗證（2026-08-19）

本次將目錄區與篩選列由固定最大寬度改為 `w-full max-w-none`，桌機保留 `lg:px-8` 的 32px 安全內距，手機維持 `px-4`。1891px Playwright／DOM 驗收結果如下：`#catalog` 左界為 0、右界為 1891，第一張卡片左界為 32、第二張卡片右界為 1859，Grid 為 2 欄，整頁 `scrollWidth` 為 1891，代表左右僅保留安全內距且沒有頁面溢出。

375px 回歸結果如下：Grid 為 1 欄，整頁 `scrollWidth` 與 viewport 同為 375；先切換第 2 頁，再輸入關鍵字、切換恐怖排序與恐怖篩選後，頁碼正確回到第 1 頁。768px 既有版面斷言亦通過，Badge 維持單行橫向容器，頁碼列與篩選列在內部可操作而不造成整頁水平溢出；production build 通過。


## 主要輔助文字提升至 14px（2026-08-19）

首頁原本使用 `text-xs` 的主要輔助文字已統一改為 Tailwind `text-sm`（14px），涵蓋導覽、Hero 輔助標記、篩選列、排序、搜尋說明、主題卡片 Badge、優缺點、分頁與 footer；主標題維持原有層級。卡片指標原本的 13px 數值也同步提升至 14px，使公開首頁最小可見字級一致為 14px。

Playwright 驗收結果：1280px 與 375px 的最小可見字級、搜尋字級與排序字級均為 14px；1280px 頁碼列高度 63px、375px 頁碼列高度 119px，頁碼控制無溢出；卡片內容與整頁均無水平溢出。14px 放大初次造成手機優缺點標籤超出卡片，已透過手機換行、`min-w-0` 與桌機橫向容器修正；Grid、真正滿版與 production build 均通過。


14px 版本補強驗收：1280px、768px、375px 的搜尋／恐怖篩選／恐怖排序／分頁重置互動均通過，三種 viewport 皆無整頁水平溢出。1891px DOM 驗收顯示第一張卡片寬 900px、高 739.5px；主題標題 1 行、店家資訊 1 行；最小可見字級 14px；整頁 `scrollWidth` 1891，沒有水平溢出。


14px 字級版本 checkpoint：`53f1f7b1`（2026-08-19）。


## 手機恢復 12px、桌機平板維持 14px（2026-08-19）

首頁主要原先為 12px 的輔助文字已改為響應式 class：手機使用 12px，`sm` 以上使用 14px；原本就為 14px 的搜尋輸入框與方法說明正文維持 14px。實測 1280px 最小可見字級與排序控制均為 14px；375px 最小可見字級與排序控制為 12px，搜尋輸入框維持 14px。1280px、768px、375px 的搜尋／恐怖篩選／恐怖排序／分頁重置均通過，整頁無水平溢出；1891px 滿版容器與兩欄 Grid 亦維持正常。


本輪補強的 1891px DOM 字級驗收結果：最小可見字級 14px、排序控制 14px、卡片店家資訊 14px、卡片寬 900px、整頁 `scrollWidth` 1891，均符合桌機維持 14px 且無水平溢出的要求。


本輪響應式字級 checkpoint：`25146cfd`（2026-08-19）。


## 主題一言物語／劇情摘要（2026-08-20）

本輪盤點發現正式資料檔目前共有 **100 筆主題**，不是 55 筆；因此摘要欄位實際補齊 100 筆。被動掃描 18 個現有來源網址後，35 筆可在官方頁面內容中定位主題名稱，但未能一致取得完整主題級劇情段落，因此資料欄位將其標示為「官方頁面主題名稱定位後的編輯重寫」，並保存來源網址與周邊摘錄；其餘 65 筆標示為「編輯部導覽短評 fallback」。所有摘要均非官方逐字引用，長度為 43–50 字。

卡片新增 `一言物語／劇情摘要` 區塊，位置在主題標題與評分／核心數據之間，採 ✦ 前綴、斜體、灰色文字與左側氧化金線；手機使用 12px、sm 以上使用 14px。跨 10 頁瀏覽器驗收完成：每頁 10 張卡片、共 100 筆，所有摘要均渲染、長度 30–50 字，1280px 整頁無水平溢出；375px 卡片渲染與整頁無溢出亦通過。搜尋、恐怖篩選、排序、分頁重置與 production build 通過。


本輪一言物語／劇情摘要 checkpoint：`983b9585`（2026-08-20）。


本輪最終一言物語 checkpoint：`367096f3`（2026-08-20）。


## 優缺點標籤與 Circle Dots 視覺優化（2026-08-21）

資料層將 100 筆主題的 400 個優缺點標籤依既有 styles、恐怖／燒腦編輯部導覽分級、人數與資料品質欄位重新整理；共有 121 個唯一標籤，單一標籤最高重複 7 次，並加入 `tag_provenance` 與透明說明，未將內容宣稱為逐字玩家評論。

前端將原本文字化 `●／○` 改為五點 Circle Dots：恐怖啟用點為 `bg-red-500 shadow-[0_0_8px_#ef4444]`，燒腦啟用點為 `bg-indigo-400 shadow-[0_0_8px_#818cf8]`，未達級距使用 `bg-slate-700`；點尺寸為手機 `w-3 h-3`、sm 以上 `w-3.5 h-3.5`，點間距為 `gap-2`，並附上 `/5` 文字與 aria-label。

驗收結果：`pnpm test` 2 個測試檔、2 個測試通過；`pnpm build` 通過。既有互動回歸在 1280／768／375px 的搜尋、恐怖篩選、恐怖排序、分頁重置與 overflow 均通過。專用 DOM 驗收確認 1891／1280／768／375px 的 `scrollWidth` 分別等於 viewport 寬度，兩組 Circle Dots 各有 5 點，首點尺寸至少 12×12px，標籤資料 100 筆完整。


## 台灣在地化語感與錯字修正（2026-08-22）

本輪將首頁 Banner、地區／主題篩選列、搜尋說明、方法區、評分提示與 footer 改為自然的台灣繁體中文；主題卡片固定使用「建議人數」「遊戲時間」「恐怖指數」「燒腦程度」，預約按鈕固定為「立即預約此主題（享獨家優惠）」。

資料清洗後，100 筆主題全部保留兩個優點與兩個缺點標籤，唯一優點標籤 61 個、唯一缺點標籤 59 個。65 筆無法精確核對的遊戲時間統一顯示「依官網為主」，相關來源提示統一為「請見官方公告」；`符核對` 與 `待核對` 均已從公開資料與首頁程式碼移除。劇情摘要改為台灣玩家較自然的語氣，並維持編輯部導覽短評 provenance，未宣稱官方逐字文案或捏造玩家評論。

驗收結果：台灣化文字腳本通過，100 筆資料完整、固定 CTA 存在、禁用提示詞為 0；Vitest 2 個測試檔／2 個測試通過，production build 通過。既有互動回歸在 1280／768／375px 的搜尋、篩選、恐怖排序、分頁重置與水平溢出均通過；視覺檢查確認 1280px 與 375px Banner、篩選列及手機橫向操作正常。

## 深度在地化洗牌、彈性標籤與我的最愛（2026-08-22）

正式 `topics.json` 已重新套用使用者指定詞庫。每筆主題依既有 styles、編輯部恐怖／燒腦分級、人數與店家評論數配對 0–2 個標籤，不強制補滿；100 筆中有 14 筆不顯示缺點、34 筆顯示 1 個缺點、52 筆顯示 2 個缺點。資料與前端已移除「符核對」「符核」「待核對」「依官網為主」「活動活動」與舊「沉浸劇情」，不確定的人數、時間或評分統一顯示「依官網公告」，風格改為「沉浸式劇情」。標籤 provenance 明確註明為依公開主題屬性與導覽分級配對，未引用或捏造 Dcard、PTT 玩家發言。

卡片評分顯示改為「⭐ X.X 工作室評分」。優缺點區依實際標籤數量顯示，單側資料會橫跨整張卡片，長標籤列使用 `overflow-x-auto` 與可見的 thin scrollbar。新增每張卡片愛心按鈕、篩選列「我的最愛」計數與只看收藏模式，收藏 ID 透過 `localStorage` 鍵 `taiwan-escape-favorites:v1` 持久保存。上一頁、下一頁與直接頁碼切換後會平滑捲到 `#catalog-grid`，實測第一張卡片頂端為 125px，位於黏性篩選列下方。

最終驗收：TypeScript 通過；Vitest 共 4 個測試檔、8 個測試通過；production build 通過。Playwright 驗證收藏切換、重新整理後持久化、只看收藏、標籤 scrollbar 與分頁定位均通過，1891／1280／768／375px 的 `scrollWidth` 均等於 viewport 寬度，沒有整頁水平溢出。

本輪自動發布 checkpoint：`06bbb9e5`。
