# 推出時間欄位視覺驗證

## 驗證日期

2026-09-07。

## 驗證結果

首頁桌面版（1280×720）維持既有夜蝕都市導覽的導覽列、英雄區與篩選工具列層次，沒有看到型別或開發伺服器錯誤。手機版（390×844，整頁截圖）中，主題卡片內新增的「推出時間」資訊列維持在主題名稱下方，窄版文字可讀且沒有造成水平溢出；推出時間未有可靠資料時顯示「尚未收錄」。

## 資料誠信備註

目前 `data/topics.json` 與 `data/venues.json` 沒有可直接核實的主題推出日期，因此本次只新增可選的 `release_time` 欄位並以空值呈現「尚未收錄」，未以資料抓取日期或推測值冒充推出時間。後續取得官方公告或可靠來源後，可直接填入月份、年份或完整日期字串。
