# Google Avatar Visual Verification

- Full-page preview shows the existing neo-noir layout remains intact after adding avatars.
- Desktop top viewport shows the signed-in account control with a circular avatar fallback/Google avatar slot, account name, and logout icon aligned without clipping.
- Full-page preview shows comment cards retain spacing and author-row alignment after adding circular avatar UI.
- Automated tests cover a Google avatar URL, anonymous fallback initials, OAuth profile picture sync, and existing comment/auth flows.
