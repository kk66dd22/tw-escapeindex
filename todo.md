# 全台密室案件庫擴充待辦

- [x] 盤點現有 venues.json 與前端資料流，定義全台地區欄位與城市索引。
- [x] 蒐集並核對目前可公開核驗的北部、中部、南部代表性真實密室工作室與官方網站。東部與離島保留為下一輪擴充範圍。
- [x] 為每家店整理地址、官方入口、主題、人數、恐怖／燒腦導覽指數與優缺點摘要。
- [x] 擴充 Python 爬蟲與快照輸出，保留來源與評價核對欄位。
- [x] 加入縣市／區域篩選、全台統計與城市導覽 UI。
- [x] 通過 TypeScript 與 production build，並完成桌面版首頁視覺驗證；互動流程可再由使用者於 Preview 實際點測。
- [x] 完成手機版視覺驗證；新版 checkpoint 與原始碼打包待保存。

## 目錄卡片視覺重構

- [x] 將卡片上半部改為封面圖、店名與大型 Google 評分並列。
- [x] 將主題詳細資訊收斂為橫向 Badge，只保留招牌主題與建議人數。
- [x] 將優缺點改為綠／紅兩欄極簡對比區塊，避免長句堆疊。
- [x] 將官方預約 CTA 改為卡片底部 100% 寬、rounded-lg 的固定收尾。
- [x] 驗證桌面與手機版卡片層級、溢出與互動狀態。

## 主題核心目錄重構

- [x] 將 venues.json 的巢狀 themes 展平成一主題一筆的前端索引模型。
- [x] 為每個主題補齊遊戲時間、建議人數、恐怖指數、燒腦程度與兩組精簡優缺點標籤；未穩定公開的遊戲時間明確標示待核對。
- [x] 重寫地區、關鍵字、人數、恐怖、燒腦與風格篩選，使結果只包含主題卡片。
- [x] 將目錄卡片改為主題名稱、店家／地區、副標籤、Icon 化核心數據與主題級預約 CTA。
- [x] 驗證恐怖篩選可跨店家比較、地區篩選正確、桌面／手機版無溢出；主題測試通過：34 場主題、15 場恐怖、10 家店家、9 場台中。

## 主題排序功能

- [x] 新增排序狀態與下拉選單，支援評分最高、恐怖指數最高、燒腦程度最高。
- [x] 讓排序套用在搜尋、地區與主題篩選後的結果集合，並對待核對評分採穩定低優先處理。
- [x] 驗證三種排序的首筆結果、篩選交集與桌面／手機版控制項排版；評分首筆 5.0、恐怖首筆 5、燒腦首筆 4，台中恐怖交集 5 場。

## 全台 100 主題熱門榜擴充

- [x] 建立 B 模式收錄規則：最多 100 個主題，以所屬店家／分店 Google 4.5 以上作為代理門檻，並在介面註明不是單一主題評分。
- [x] 完成官方來源核對；目前以不捏造為原則輸出 55 筆核驗主題：台北 29、台中 9、台南 4、高雄 13。公開來源不足以安全填滿 100 筆，缺口已保留在研究紀錄。
- [x] 生成核驗版 topics.json，主題 ID 唯一、去重，僅保留代理 Google 評價 4.5 以上項目。
- [x] 將複合篩選改為可同時套用城市、恐怖、燒腦、人數與風格條件，並保留排序功能。
- [x] 瘦身主題卡片、優缺點標籤化，加入 content-visibility 懶渲染提示；production build、桌面與手機版驗證完成。

## Google Places API 補齊流程

- [x] 確認採用 Manus 內建 Google Maps／Places proxy；不需要也不暴露外部 API key。
- [x] 建立獨立 places_candidates.json 掃描輸出，並保留原 55 筆主題作為正式回退資料。
- [x] 透過 admin-only tRPC procedure 與內建 Maps proxy 查詢 62 個候選，取得名稱、地址、評分、評論數、Place ID、營運狀態與核對時間。
- [x] 將可穩定匹配的 Places metadata 合併到 12 家 venues 與 53 筆既有 topics；主題數量維持 55，未把只有店家頁的候選冒充成單一遊戲主題。
- [x] Places 單元測試、既有 auth 測試、TypeScript、production build 均通過；前端仍以靜態 55 筆索引載入，API 失敗不影響公開目錄。

## 62 候選展開至 100 主題

- [x] 盤點 62 個 Places 候選與現有 55 個主題的名稱、城市、官方網址與重複關係。
- [x] 完成 62 家候選逐店狀態審核清單；13 家有官方頁核對紀錄，49 家明確標示待核對，未把待核對者冒充已核驗。
- [x] 將可核對主題展平為一主題一筆並標註 100 筆；65 筆遊戲時間保留「待核對」語意，未宣稱資料完整。
- [x] 去重並將正式 topics.json 控制為 100 筆；同名不同分館以 venue／city／name 組合識別，未核對主題未填入。
- [x] 驗證主題 ID 與 venue／city／name 唯一性、官方連結、代理評分門檻、測試、production build，以及桌面／手機版 100 筆載入排版。

## 資料品質補強（100 主題版）

- [x] 建立 62 個 Places 候選的逐店核對清單，記錄官方主題頁 URL、核對日期、是否已展開與未納入原因。
- [x] 完成遊戲時間品質註記：35 筆有官方頁依據，65 筆明確標示待官方頁核對，並寫入研究紀錄。
- [x] 將新增主題的恐怖／燒腦與優缺點標記為「編輯部導覽分級」，並在資料欄位中區分非官方難度／評價。

## 主題目錄分頁

- [x] 將主題結果切分為每頁 10 組，計算總頁數與目前頁碼。
- [x] 加入上一頁、下一頁與頁碼狀態，處理首尾頁 disabled 狀態。
- [x] 搜尋、地區、複合篩選或排序變更時自動回到第 1 頁。
- [x] 驗證 100 組資料共 10 頁、篩選結果分頁正確，以及桌面／手機版控制項不溢出；測試與 production build 通過。

## 頁碼快速跳頁

- [x] 在分頁控制中顯示可點選的 1–10 頁碼。
- [x] 高亮目前頁碼並維持上一頁／下一頁 disabled 狀態。
- [x] 驗證篩選排序變更後頁碼回到第 1 頁，以及手機版頁碼列可橫向操作且不溢出；型別、建置、測試與視覺驗證通過。

## 分頁驗收補強

- [x] 新增可重跑的分頁互動測試：先切到第 2 頁，再改變搜尋／地區／篩選／排序，確認頁碼自動回到第 1 頁。
- [x] 補做手機版頁碼列的程式化檢查，確認 1–10 頁碼可在容器內橫向操作且不造成頁面水平溢出；測試確認 `overflow-x-auto` 防護存在。

## 真實互動／版面驗收補強

- [x] 新增真正掛載首頁的 Playwright 分頁互動測試；第 2 頁後變更搜尋、地區、恐怖篩選與排序均回到第 1 頁，並可直接跳到第 10 頁。
- [x] 新增手機 viewport 的 Playwright DOM 版面測試；頁面 scrollWidth 375 等於 viewport 375，頁碼控制保留 overflow-x-auto 且無整頁水平溢出。

## 回到頂部懸浮按鈕

- [x] 新增右下角懸浮按鈕，僅在頁面向下滾動超過 520px 後顯示。
- [x] 點擊按鈕以平滑行為回到頁面最上方，並提供 aria-label、title 與可見 focus 狀態。
- [x] 以 Chromium 驗證桌面／手機版顯示切換與點擊後 scrollY < 8；測試、型別檢查與 production build 通過。

## 品牌名稱更新

- [x] 將頁首品牌文字「全台主題案件庫」改為「全台密室逃脫案件庫」。
- [x] 同步更新網站 title、description metadata、替代文字與可見品牌相關文案。
- [x] 搜尋 client 原始碼確認無舊品牌名稱殘留；測試、TypeScript 與 production build 通過。

## 列表上方關鍵字搜尋

- [x] 在主題列表上方新增獨立關鍵字搜尋框與清除按鈕。
- [x] 搜尋即時套用至主題、店家、城市與風格，並沿用現有 query 狀態讓結果變更時回到第 1 頁。
- [x] 以 Chromium 驗證台中關鍵字即時過濾、清除按鈕、桌面／手機版不溢出；測試、型別檢查與 production build 通過。

## 移除 Hero 搜尋列

- [x] 移除 Hero 區上方搜尋輸入框與清除按鈕，保留 query 狀態供列表搜尋使用。
- [x] 確認列表上方搜尋框成為唯一搜尋入口，搜尋、篩選、排序與分頁仍正常。
- [x] 驗證桌面／手機版唯一搜尋入口與版面；Chromium 搜尋測試、TypeScript 與 production build 通過。

## 移除搜尋列回歸驗收

- [x] 以 Chromium 驗證列表搜尋＋恐怖篩選＋恐怖排序＋頁碼跳轉與搜尋變更回到第 1 頁的整合流程。
- [x] 驗證頁面只存在一個搜尋輸入框，且唯一輸入框為 `#catalog-search` 列表搜尋框。

## 桌面兩欄卡片布局

- [x] 將桌面主題卡片 Grid 從三欄改為兩欄，保留手機單欄。
- [x] 檢查兩欄下封面圖、數據區、Badge、優缺點與 CTA 的閱讀層級；桌面截圖確認卡片區獲得更寬的內容空間。
- [x] 驗證搜尋、篩選、排序、分頁與桌面／手機版無水平溢出；Grid 測試確認 1280px 為 2 欄、375px 為 1 欄。

## 兩欄版型回歸驗收

- [x] 重新執行桌面兩欄版的搜尋、篩選、排序與分頁整合測試；1280px Chromium 通過。
- [x] 補做手機 375px 下的搜尋、篩選、排序、分頁互動與水平溢出驗證；Chromium 通過。

## 兩欄版型分頁互動補強

- [x] 在 1280px 實際點擊第 2 頁，再結合搜尋、恐怖篩選與恐怖排序驗證回到第 1 頁。
- [x] 在 375px 實際點擊第 2 頁，再操作搜尋、恐怖篩選與恐怖排序；確認回到第 1 頁且無水平溢出。

## 主題卡片文字可讀性

- [x] 放大主題名稱、店家／地區與評分資訊，建立清楚的卡片文字層級。
- [x] 放大核心數據、主題 Badge、優缺點標籤與 CTA 文字，調整行距與間距避免擁擠。
- [x] 驗證桌面兩欄與手機單欄的文字可讀性、水平溢出、互動與 production build；Grid 與搜尋／篩選／排序／分頁回歸測試通過。

## 全站最小字級 12px

- [x] 將全站主要 UI 的 10px／11px 文字提升至 12px，包含導覽、篩選、卡片輔助資訊、頁碼與 footer。
- [x] 保留標題與品牌的既有層級，避免單純全域放大造成視覺失衡。
- [x] 驗證 1280px 與 375px 的文字換行、搜尋／篩選／排序／分頁互動、水平溢出與 production build；全站原始碼未殘留 9／10／11px UI 字級。

## 全站字級版面補強

- [x] 重新做 1280px 與 375px 字級放大後視覺驗證，確認導覽、篩選器、卡片、頁碼與 footer 未擠壓。
- [x] 新增可重跑的瀏覽器文字版面檢查；1280px／375px 最小可見字級均為 12px，搜尋與排序為 14／12px，卡片／頁面／頁碼控制無水平溢出。

## 桌機兩欄滿版布局

- [x] 將目錄外層改為 max-w-[95%] lg:max-w-[1400px] mx-auto，減少桌機左右贅空。
- [x] 嚴格維持 grid-cols-1 lg:grid-cols-2 gap-6，卡片維持 p-6，主題標題採 text-2xl sm:text-3xl font-bold。
- [x] 讓優缺點 Badge 在寬卡片中於 sm 以上橫向舒展，手機／平板維持單欄與 w-full px-4。
- [x] 驗證桌機、平板、手機的搜尋／篩選／排序／分頁、文字換行與水平溢出；1280px 兩欄、768／375px 單欄，build 與互動測試通過。

## 平板 768px 回歸驗收

- [x] 新增 768px viewport 的可重跑互動測試，覆蓋搜尋、恐怖篩選、恐怖排序、分頁與頁碼重置。
- [x] 新增 768px viewport 的版面／溢出檢查，確認單欄卡片、Badge、頁碼與控制列無異常換行或水平溢出；Grid 與互動測試均通過。

## 平板版面斷言補強

- [x] 新增 768px 瀏覽器版面斷言，檢查 Badge、頁碼列與篩選控制列的 scrollWidth／clientWidth、行數與高度，確認無異常擠壓。
- [x] 重新執行 768px 平板驗收並保存明確輸出：單欄卡片、Badge 皆單行 26px 且 overflow-x-auto 可控、頁碼列 698／698px、控制列 768／1440px 可橫向操作、整頁 768／768px 無溢出。

## 真正桌機滿版修正

- [x] 移除目錄區桌機 `max-w-[95%]` 與 `lg:max-w-[1400px]` 限制，改為 `w-full max-w-none`。
- [x] 保留桌機 32px 左右安全 padding，讓兩欄卡片延伸至可用寬度且不貼邊；手機／平板維持 `w-full px-4` 與單欄。
- [x] 驗證 1891px 超寬桌機、768px 與 375px 的左右留白、Grid、搜尋／篩選／排序／分頁及水平溢出；768px 斷言與 production build 通過，超寬桌機截圖確認卡片區已延展。

## 真正滿版數值驗收補強

- [x] 新增 1891px Playwright／DOM 斷言，量測 catalog 容器左右內距、Grid 欄數與整頁 scrollWidth，保存明確數值輸出。
- [x] 重新執行 375px 的搜尋／篩選／排序／分頁與 overflow 回歸測試，確認真正滿版修改後手機互動仍正常。
- [x] 將 1891px 與 375px 驗證結果寫入 research/layout-validation.md，附上可核對數值。

## 主要輔助文字提升至 14px

- [x] 將目前主要 UI 的 12px 輔助文字提升至 14px，包含導覽、篩選、搜尋說明、卡片輔助資訊、Badge、優缺點、頁碼與 footer；標題層級維持不變。
- [x] 驗證 1891px、1280px、768px 與 375px 的文字換行、卡片高度、搜尋／篩選／排序／分頁互動及整頁水平溢出；最小可見字級 14px，手機標籤溢出已修正。
- [x] 更新 layout-validation.md 並保存 14px 字級版本 checkpoint。

## 14px 字級驗收補強

- [x] 重新執行 768px 的搜尋／恐怖篩選／排序／分頁重置回歸測試，確認 14px 字級後互動與無溢出仍成立。
- [x] 新增 1891px DOM／Playwright 驗證，量測 14px 字級後卡片高度、關鍵文字換行與整頁無溢出，保存明確數值輸出。
- [x] 完成上述驗收後保存新的 14px 字級 checkpoint，並把 checkpoint 版本號 `53f1f7b1` 記錄到 research/layout-validation.md。

## 手機恢復 12px、桌機維持 14px

- [x] 將首頁主要輔助文字在手機 viewport 覆寫為 12px，桌機與平板維持 14px；標題層級不變。
- [x] 修正手機版可能產生的卡片、Badge、頁碼與頁面水平溢出，保留搜尋／篩選／排序／分頁互動。
- [x] 驗證 1891px、1280px、768px 與 375px 的實際字級、版面與互動，更新研究紀錄並保存 checkpoint。

## 響應式字級驗收補強

- [x] 新增 1891px DOM 字級斷言，明確輸出最小可見字級、排序控制字級與卡片文字字級。
- [x] 保存本輪手機 12px／桌機平板 14px checkpoint，並將新版本號 `25146cfd` 記錄到 research/layout-validation.md。

## 主題一言物語／劇情摘要

- [x] 盤點實際 100 筆主題的官方劇情來源與現有資料欄位，保留來源與摘要 provenance；18 個來源網址中有 35 筆可由官方頁面定位主題名稱。
- [x] 為實際 100 筆主題補齊 1–2 句、43–50 字摘要；35 筆標示官方頁面名稱定位後的編輯重寫，65 筆使用明確標示的編輯部導覽短評 fallback，不冒充官方文案。
- [x] 在主題標題與核心數據之間加入「一言物語／劇情摘要」區塊；桌機維持兩欄、手機維持單欄，摘要使用斜體灰色引言樣式與 ✦ 前綴。
- [x] 驗證 100 筆摘要完整性、摘要長度、搜尋／篩選／排序／分頁、1891／1280／768／375px 版面與水平溢出，更新研究紀錄並保存 checkpoint。

## 劇情摘要 provenance 與全資料驗收補強

- [x] 將摘要說明與 provenance 改為透明表述：35 筆僅為官方頁面主題名稱定位後的編輯重寫，65 筆為編輯部 fallback，均不宣稱官方逐字劇情。
- [x] 新增跨 10 頁／100 筆的瀏覽器驗收，確認每筆摘要都能渲染、長度 30–50 字且搜尋／篩選／排序／分頁後仍不溢出。
- [x] 將本輪摘要來源快照、摘要統計與 UI 驗收結果寫入 research/layout-validation.md，並保存劇情摘要 checkpoint；最終版本 `367096f3` 已記錄。

## 優缺點標籤多樣化與 Circle Dots 視覺優化

- [x] 盤點 100 筆主題的優缺點標籤重複率與現有恐怖／燒腦指示器 class，保留編輯部導覽分級的透明語意；現有指示器為文字化 ●／○。
- [x] 依既有 styles、恐怖／燒腦導覽分級、人數與資料品質欄位提升優點／缺點標籤多樣性；100 筆共 400 個標籤、121 個唯一標籤、最大單一標籤重複 7 次，並加入透明 tag provenance，不捏造玩家評論。
- [x] 放大恐怖／燒腦 Circle Dots 至 `w-3 h-3`、sm 以上 `w-3.5 h-3.5`，增加 `gap-2` 點間距、霓虹紅／藍紫光暈與暗灰空心點對比。
- [x] 驗證 1891／1280／768／375px 的標籤換行、Circle Dots 可讀性、搜尋／篩選／排序／分頁與水平溢出；專用 DOM、既有互動、Vitest 與 production build 均通過，研究紀錄已更新。
- [x] 建立不含 node_modules、dist、.git 與暫存檔的完整原始碼 ZIP `/home/ubuntu/taipei-escape-guide-source.zip`，於最終交付時附上。
