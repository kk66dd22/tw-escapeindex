/**
 * Style reminder: 夜蝕都市 Neo-noir editorial；煤黑、氧化金、冷霧藍。
 * Content model: 一張卡片 = 一個可被比較與預約的密室遊戲主題。
 */
import { useEffect, useMemo, useState } from "react";
import { ArrowDownUp, Brain, Check, Clock3, Compass, ExternalLink, Flame, MapPin, Search, Star, Users, X } from "lucide-react";
import topics from "../../../data/topics.json";

type Filter = "all" | "2-4" | "5-plus" | "beginner" | "brainy" | "horror" | "puzzle";
type SortMode = "rating" | "horror" | "brain";
const PAGE_SIZE = 10;
const cityFilters = ["全台", "台北市", "台中市", "嘉義市", "台南市", "高雄市"] as const;
const filters: { id: Filter; label: string }[] = [
  { id: "all", label: "全部主題" }, { id: "2-4", label: "2–4 人" }, { id: "5-plus", label: "5 人以上" },
  { id: "beginner", label: "新手入門" }, { id: "brainy", label: "重度燒腦" }, { id: "horror", label: "恐怖驚悚" }, { id: "puzzle", label: "機關解謎" },
];

function ratingLabel(rating: number | null) { return rating ? rating.toFixed(1) : "待核對"; }
function maxPlayers(value: string) { return Number(value.split("–")[1] ?? value.split("-")[1] ?? 99); }
function scoreDots(value: number | null) { return value ? "●".repeat(value) + "○".repeat(5 - value) : "待核對"; }
function matchesFilter(topic: typeof topics[number], filter: Filter) {
  if (filter === "beginner") return maxPlayers(topic.players) <= 6 && (topic.brain ?? 5) <= 3;
  if (filter === "brainy") return (topic.brain ?? 0) >= 4;
  if (filter === "horror") return (topic.horror ?? 0) >= 4;
  if (filter === "puzzle") return topic.styles.some((style) => style.includes("機關") || style.includes("解謎"));
  if (filter === "2-4") return maxPlayers(topic.players) <= 4;
  if (filter === "5-plus") return maxPlayers(topic.players) >= 5;
  return true;
}

export default function Home() {
  const [query, setQuery] = useState("");
  const [activeFilters, setActiveFilters] = useState<Filter[]>([]);
  const [activeCity, setActiveCity] = useState<string>("全台");
  const [sortMode, setSortMode] = useState<SortMode>("rating");
  const [page, setPage] = useState(1);
  const filtered = useMemo(() => {
    const matched = topics.filter((topic) => {
    const q = query.toLowerCase().trim();
    const haystack = [topic.name, topic.venue_name, topic.city, topic.district, ...topic.styles].join(" ").toLowerCase();
    const textMatch = !q || haystack.includes(q);
    const cityMatch = activeCity === "全台" || topic.city === activeCity;
    const filterMatch = activeFilters.every((filter) => matchesFilter(topic, filter));
      return textMatch && cityMatch && filterMatch;
    });
    return [...matched].sort((a, b) => {
      const aValue = sortMode === "rating" ? (a.google_rating ?? -1) : sortMode === "horror" ? (a.horror ?? -1) : (a.brain ?? -1);
      const bValue = sortMode === "rating" ? (b.google_rating ?? -1) : sortMode === "horror" ? (b.horror ?? -1) : (b.brain ?? -1);
      return bValue - aValue;
    });
  }, [query, activeFilters, activeCity, sortMode]);
  const pageCount = Math.max(1, Math.ceil(filtered.length / PAGE_SIZE));
  const visibleTopics = useMemo(() => filtered.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE), [filtered, page]);
  useEffect(() => { setPage(1); }, [query, activeFilters, activeCity, sortMode]);
  useEffect(() => { if (page > pageCount) setPage(pageCount); }, [page, pageCount]);
  const toggleFilter = (filter: Filter) => setActiveFilters((current) => current.includes(filter) ? current.filter((item) => item !== filter) : [...current, filter]);

  return <div className="min-h-screen bg-[#0c0e0d] text-[#e8e4db] selection:bg-[#c89b5c] selection:text-[#0c0e0d]">
    <div className="pointer-events-none fixed inset-0 z-0 opacity-[0.06] [background-image:linear-gradient(rgba(200,155,92,.35)_1px,transparent_1px),linear-gradient(90deg,rgba(200,155,92,.35)_1px,transparent_1px)] [background-size:54px_54px]" />
    <header className="relative z-10 border-b border-white/10 bg-[#0c0e0d]/90 backdrop-blur-md"><div className="mx-auto flex max-w-[1400px] items-center justify-between px-5 py-4 lg:px-10"><a href="#top" className="flex items-center gap-3"><img src="/manus-storage/brand-sigil_8091f515.png" className="h-10 w-10 object-contain" alt="裂門案件庫圖騰" /><span className="font-mono text-[11px] uppercase tracking-[.3em] text-[#c89b5c]">The Escape Index<br /><b className="font-sans text-sm tracking-[.16em] text-[#e8e4db]">全台主題案件庫</b></span></a><nav className="hidden items-center gap-8 font-mono text-[10px] uppercase tracking-[.2em] text-white/50 md:flex"><a href="#catalog" className="hover:text-[#c89b5c]">主題目錄</a><a href="#method" className="hover:text-[#c89b5c]">評比方法</a></nav><div className="font-mono text-[10px] tracking-widest text-white/40">ROOM FILE / 03</div></div></header>
    <main id="top" className="relative z-10">
      <section className="relative overflow-hidden border-b border-white/10"><img src="/manus-storage/hero-night-alley_363700f4.png" alt="夜晚台灣城市巷弄中的神秘入口" className="absolute inset-0 h-full w-full object-cover opacity-55" /><div className="absolute inset-0 bg-gradient-to-r from-[#0c0e0d] via-[#0c0e0d]/80 to-transparent" /><div className="mx-auto grid min-h-[600px] max-w-[1400px] items-end gap-12 px-5 pb-16 pt-20 lg:grid-cols-[1fr_360px] lg:px-10 lg:pb-24"><div className="relative max-w-3xl"><div className="mb-7 flex items-center gap-3 font-mono text-[10px] uppercase tracking-[.3em] text-[#c89b5c]"><span className="h-px w-10 bg-[#c89b5c]" /> Taiwan / Room Index 03</div><h1 className="font-serif text-5xl font-black leading-[.98] tracking-tight text-[#f3efe7] sm:text-7xl lg:text-[92px]">先選一場，<br /><em className="font-normal text-[#c89b5c]">再決定去哪裡。</em></h1><p className="mt-8 max-w-xl text-base leading-8 text-white/70 sm:text-lg">不是先找店家，而是先找到今晚真正想玩的那一個主題。全台 {topics.length} 場高評價主題，拆開比較。</p><div className="relative mt-10 max-w-xl"><Search className="absolute left-5 top-1/2 h-5 w-5 -translate-y-1/2 text-[#c89b5c]" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="搜尋主題、店家、城市或風格..." className="w-full border border-[#c89b5c]/60 bg-[#0c0e0d]/80 py-4 pl-14 pr-12 font-sans text-sm text-white outline-none backdrop-blur-sm focus:border-[#c89b5c] focus:ring-1 focus:ring-[#c89b5c]" />{query && <button onClick={() => setQuery("")} className="absolute right-4 top-1/2 -translate-y-1/2 text-white/50 hover:text-white"><X size={16} /></button>}</div></div><aside className="hidden border-l border-[#c89b5c]/50 pl-7 lg:block"><div className="mb-10 font-mono text-[10px] uppercase tracking-[.25em] text-white/40">Index / Snapshot</div><div className="space-y-7 font-mono"><div><div className="text-4xl text-[#c89b5c]">{topics.length}</div><div className="mt-1 text-[10px] uppercase tracking-widest text-white/45">Rooms indexed</div></div><div><div className="text-4xl text-[#c89b5c]">05</div><div className="mt-1 text-[10px] uppercase tracking-widest text-white/45">Cities mapped</div></div><div><div className="text-4xl text-[#c89b5c]">07</div><div className="mt-1 text-[10px] uppercase tracking-widest text-white/45">Decision filters</div></div></div></aside></div></section>
      <section className="sticky top-0 z-20 border-b border-white/10 bg-[#111412]/95 backdrop-blur-xl"><div className="mx-auto flex max-w-[1400px] items-center gap-3 overflow-x-auto px-5 py-4 lg:px-10"><span className="mr-2 shrink-0 font-mono text-[10px] uppercase tracking-[.25em] text-[#c89b5c]">Region /</span><div className="flex shrink-0 items-center gap-1 border-r border-white/10 pr-4">{cityFilters.map((city) => <button key={city} onClick={() => setActiveCity(city)} className={`shrink-0 px-2 py-2 font-mono text-[10px] tracking-wider transition ${activeCity === city ? "text-[#c89b5c]" : "text-white/45 hover:text-white"}`}>{city === "全台" ? city : city.replace("市", "")}</button>)}</div><span className="mr-2 shrink-0 font-mono text-[10px] uppercase tracking-[.25em] text-[#c89b5c]">Room /</span>{filters.map((filter) => <button key={filter.id} onClick={() => filter.id === "all" ? setActiveFilters([]) : toggleFilter(filter.id)} className={`shrink-0 border px-4 py-2 font-mono text-[10px] transition ${(filter.id === "all" ? activeFilters.length === 0 : activeFilters.includes(filter.id)) ? "border-[#c89b5c] bg-[#c89b5c] text-[#0c0e0d]" : "border-white/15 text-white/55 hover:border-[#c89b5c]/70 hover:text-[#c89b5c]"}`}>{filter.label}</button>)}<div className="ml-auto flex shrink-0 items-center gap-2 border-l border-white/10 pl-3"><ArrowDownUp size={13} className="text-[#c89b5c]" /><label htmlFor="room-sort" className="sr-only">排序主題</label><select id="room-sort" value={sortMode} onChange={(e) => setSortMode(e.target.value as SortMode)} className="appearance-none border border-white/15 bg-[#151917] px-3 py-2 font-mono text-[10px] text-white/70 outline-none transition hover:border-[#c89b5c]/70 focus:border-[#c89b5c]"><option value="rating">評分最高</option><option value="horror">恐怖指數最高</option><option value="brain">燒腦程度最高</option></select></div><span className="hidden shrink-0 font-mono text-[10px] text-white/40 sm:block">{filtered.length.toString().padStart(2, "0")} ROOMS MATCHED</span></div></section>
      <section id="catalog" className="mx-auto max-w-[1400px] px-5 py-20 lg:px-10 lg:py-28"><div className="mb-14 flex items-end justify-between gap-5"><div><div className="mb-4 flex items-center gap-3 font-mono text-[10px] uppercase tracking-[.3em] text-[#5e8b92]"><span className="h-px w-8 bg-[#5e8b92]" /> Room files</div><h2 className="font-serif text-4xl font-bold text-[#f3efe7] sm:text-5xl">每一場，單獨比較。<br /><span className="text-white/45">找到你的今晚。</span></h2></div><div className="hidden text-right font-mono text-[10px] leading-6 tracking-wider text-white/35 sm:block">LAST INDEXED<br />19 AUG 2026 / TOPIC FIRST</div></div>{filtered.length === 0 ? <div className="border border-dashed border-white/20 py-24 text-center"><Compass className="mx-auto mb-5 text-[#c89b5c]" size={30} /><p className="font-serif text-2xl">沒有符合條件的主題</p><button onClick={() => { setActiveFilters([]); setActiveCity("全台"); setQuery(""); }} className="mt-5 font-mono text-xs tracking-widest text-[#c89b5c] underline underline-offset-4">清除線索</button></div> : <><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{visibleTopics.map((topic, index) => <TopicCard key={topic.id} topic={topic} index={(page - 1) * PAGE_SIZE + index} />)}</div>{filtered.length > 0 && <div className="mt-10 flex flex-col items-center justify-between gap-4 border-t border-white/10 pt-6 sm:flex-row"><div className="font-mono text-[10px] tracking-widest text-white/40">PAGE {String(page).padStart(2, "0")} / {String(pageCount).padStart(2, "0")} · SHOWING {((page - 1) * PAGE_SIZE) + 1}–{Math.min(page * PAGE_SIZE, filtered.length)} OF {filtered.length}</div><div className="flex items-center gap-2"><button type="button" disabled={page === 1} onClick={() => setPage((current) => Math.max(1, current - 1))} className="border border-white/15 px-4 py-2 font-mono text-[10px] tracking-wider text-white/65 transition hover:border-[#c89b5c] hover:text-[#c89b5c] disabled:cursor-not-allowed disabled:opacity-25">← 上一頁</button><span className="min-w-10 text-center font-mono text-xs text-[#c89b5c]">{page}</span><button type="button" disabled={page === pageCount} onClick={() => setPage((current) => Math.min(pageCount, current + 1))} className="border border-[#c89b5c]/60 bg-[#c89b5c] px-4 py-2 font-mono text-[10px] font-bold tracking-wider text-[#0c0e0d] transition hover:bg-[#e0bd83] disabled:cursor-not-allowed disabled:opacity-25">下一頁 →</button></div></div>}</>}</section>
      <section id="method" className="border-y border-white/10 bg-[#111412] py-20"><div className="mx-auto grid max-w-[1400px] gap-12 px-5 lg:grid-cols-[.8fr_1.2fr] lg:px-10"><div><div className="mb-4 font-mono text-[10px] uppercase tracking-[.3em] text-[#c89b5c]">01 / Method</div><h2 className="font-serif text-4xl font-bold">主題先行。<br /><span className="text-white/45">店家只是座標。</span></h2></div><div className="grid gap-8 sm:grid-cols-3"><div><Users className="mb-5 text-[#c89b5c]" size={20} /><h3 className="mb-2 font-bold">比較隊伍</h3><p className="text-sm leading-7 text-white/45">人數直接對照，不讓店家品牌遮住真正選項。</p></div><div><Flame className="mb-5 text-[#c89b5c]" size={20} /><h3 className="mb-2 font-bold">拆開恐怖</h3><p className="text-sm leading-7 text-white/45">恐怖與燒腦分開標記，跨店家比較才有意義。</p></div><div><Check className="mb-5 text-[#c89b5c]" size={20} /><h3 className="mb-2 font-bold">直達主題</h3><p className="text-sm leading-7 text-white/45">每張卡都直達該店官方預約入口，核對檔期與票價。</p></div></div></div></section>
    </main><footer className="border-t border-white/10 px-5 py-8 lg:px-10"><div className="mx-auto flex max-w-[1400px] flex-col justify-between gap-4 font-mono text-[10px] uppercase tracking-[.2em] text-white/35 sm:flex-row"><span>© 2026 The Escape Index / Taiwan</span><span>Built for curious teams</span></div></footer>
  </div>;
}

function TopicCard({ topic, index }: { topic: typeof topics[number]; index: number }) {
  const image = index % 3 === 0 ? "/manus-storage/card-archival-room_28840e3c.png" : index % 3 === 1 ? "/manus-storage/card-ritual-basement_7b06bae4.png" : "/manus-storage/card-clockwork-lab_7ed1438f.png";
  return <article style={{ contentVisibility: "auto", containIntrinsicSize: "420px" }} className="group flex min-h-[420px] flex-col overflow-hidden border border-white/10 bg-[#151917] transition duration-300 hover:-translate-y-1 hover:border-[#c89b5c]/70"><div className="relative h-44 overflow-hidden"><img src={image} alt={`${topic.name} 主題氛圍`} className="h-full w-full object-cover opacity-65 transition duration-500 group-hover:scale-[1.04] group-hover:opacity-85" /><div className="absolute inset-0 bg-gradient-to-t from-[#151917] via-transparent to-transparent" /><span className="absolute left-4 top-4 border border-[#c89b5c]/60 bg-[#0c0e0d]/85 px-2 py-1 font-mono text-[9px] tracking-[.2em] text-[#c89b5c]">ROOM {String(index + 1).padStart(2, "0")}</span><span className="absolute right-4 top-4 bg-[#0c0e0d]/80 px-2 py-1 font-mono text-[9px] tracking-wider text-white/60">{topic.city.replace("市", "")} / {topic.district}</span></div><div className="flex flex-1 flex-col p-5"><div className="font-mono text-[10px] tracking-wider text-[#5e8b92]">{topic.venue_name} · {topic.city}</div><h3 className="mt-2 font-serif text-2xl font-bold leading-tight text-[#f3efe7]">《{topic.name}》</h3><div className="mt-2 flex items-center gap-1 font-mono text-sm text-[#c89b5c]"><Star size={15} fill="currentColor" /> <strong>{ratingLabel(topic.google_rating)}</strong><span className="text-[9px] text-white/35">Google / 分店代理</span></div><div className="mt-5 grid grid-cols-2 gap-2 border-y border-white/10 py-4"><Metric icon={<Users size={15} />} label="建議人數" value={`${topic.players}人`} /><Metric icon={<Clock3 size={15} />} label="遊戲時間" value={topic.duration} /><Metric icon={<Flame size={15} />} label="恐怖指數" value={scoreDots(topic.horror)} accent /><Metric icon={<Brain size={15} />} label="燒腦程度" value={scoreDots(topic.brain)} /></div><div className="mt-4 flex flex-wrap gap-1.5">{topic.styles.slice(0, 3).map((style) => <span key={style} className="border border-[#5e8b92]/35 bg-[#202925] px-2 py-1 font-mono text-[9px] text-[#9fbab5]">{style}</span>)}</div><div className="mt-4 grid grid-cols-2 gap-2 text-[10px]"><div className="border border-[#6e9c82]/25 bg-[#173025]/45 p-3"><div className="mb-2 font-mono text-[9px] tracking-widest text-[#8fca9d]">👍 優點標籤</div><div className="flex flex-wrap gap-1.5"><span className="border border-[#6e9c82]/30 px-2 py-1 text-[#c6d7ca]">{topic.pros[0].split("，")[0].slice(0, 14)}</span><span className="border border-[#6e9c82]/30 px-2 py-1 text-[#c6d7ca]">{topic.pros[1].split("，")[0].slice(0, 14)}</span></div></div><div className="border border-[#a66b63]/25 bg-[#351e20]/45 p-3"><div className="mb-2 font-mono text-[9px] tracking-widest text-[#d99488]">👎 缺點標籤</div><div className="flex flex-wrap gap-1.5"><span className="border border-[#a66b63]/30 px-2 py-1 text-[#e0c5c0]">{topic.cons[0].split("，")[0].slice(0, 14)}</span><span className="border border-[#a66b63]/30 px-2 py-1 text-[#e0c5c0]">{topic.cons[1].split("，")[0].slice(0, 14)}</span></div></div></div><a href={topic.booking_url} target="_blank" rel="noreferrer" className="mt-5 flex w-full items-center justify-between rounded-lg bg-[#c89b5c] px-4 py-3.5 font-mono text-[10px] font-bold tracking-wider text-[#0c0e0d] transition hover:bg-[#e0bd83] active:scale-[.98]">立即預約此主題（享獨家優惠）<ExternalLink size={15} /></a></div></article>;
}

function Metric({ icon, label, value, accent = false }: { icon: React.ReactNode; label: string; value: string; accent?: boolean }) {
  return <div className="min-w-0"><div className="flex items-center gap-1.5 font-mono text-[9px] tracking-wider text-white/40">{icon}{label}</div><div className={`mt-2 truncate font-mono text-[11px] ${accent ? "text-[#d99488]" : "text-[#d5e0dc]"}`}>{value}</div></div>;
}
